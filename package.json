{
  "name": "prompt-cache-economics",
  "version": "0.1.0",
  "private": false,
  "description": "Prompt caching is a KV-cache trick with real GPU economics. The same mistake — dynamic content in the cached prefix — bills you three different ways across OpenAI, Anthropic, and Gemini. A reproducible benchmark that measures it, on a toy dungeon-master agent.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "eval": "tsx src/run.ts",
    "tokens": "tsx src/tokens.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@google/genai": "^0.7.0",
    "dotenv": "^16.4.5",
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
