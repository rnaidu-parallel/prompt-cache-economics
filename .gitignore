node_modules/
.env
*.log
# results/ is committed as evidence (token counts + costs, nothing sensitive)

# local verification screenshots
web/_shot-*.png

# generated social assets (the mp4 is a deliverable, not source)
media/
web/frames/

# vercel project link
web/.vercel
